# 运行期数据(SQLite + 上传文件),绝不入库
data/
uploads/
*.db
*.db-wal
*.db-shm

# 编译产物
bbs
bbs-linux
bbs_*.tar.gz
checksums.txt

# 本地调试与冒烟用的临时目录(截图、临时库、补丁、演示数据脚本)
.smoke/

# 本地配置与密钥,别提交
.env
.env.*
!.env.example
*.pem
*.key
